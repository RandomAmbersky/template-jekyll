services:
  jeckyll-server:
    environment:
      - MODE_ENV
    env_file:
      - ".env"
    build: .
    ports:
      - "35729:35729"
      - "4000:4000"
    # command: "tail -f /dev/null"
    volumes:
      - ./:/src
